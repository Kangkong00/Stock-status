# 윈도우 배치 파일은 반드시 CRLF 로 유지되어야 한다.
# LF 로 바뀌면 cmd 가 괄호 블록을 잘못 읽어 "배치 파일이 아닙니다" 오류가 난다.
*.bat -text
*.cmd -text
*.command text eol=lf
*.sh      text eol=lf
*.db      binary
*.xlsx    binary
*.xlsm    binary
